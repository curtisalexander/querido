# Python
__pycache__/
*.py[codz]
*$py.class
*.egg-info/
*.egg
/dist/
build/

# Virtual environments
.venv/
venv/

# uv
# uv.lock is committed for reproducibility

# Test / coverage
.pytest_cache/
.coverage
.coverage.*
htmlcov/
coverage.xml

# Test data — fully reproducible via: uv run python scripts/init_test_data.py
data/

# Local qdo state (sessions, cached metadata written during dev or tests).
# Real repo-level metadata should live under a subdirectory that isn't
# named `.qdo`, so this ignore doesn't mask intentional commits.
.qdo/

# Self-hosting eval result logs (scripts/eval_skill_files.py). Per-run
# JSON results are local artifacts — commit an explicit baseline file
# manually when snapshotting.
scripts/eval_results/

# Tool caches
.ruff_cache/
.mypy_cache/

# Environment / secrets
.env
.envrc

# IDE
.idea/
.vscode/

# macOS
.DS_Store

# Claude
.claude/
